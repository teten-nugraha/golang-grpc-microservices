Untuk membangun semua Docker image, jalankan:

bash

make build

Untuk menerapkan semua service ke Kubernetes, jalankan:

bash

make deploy

Untuk menjalankan semuanya (build dan deploy), jalankan:

bash

make all